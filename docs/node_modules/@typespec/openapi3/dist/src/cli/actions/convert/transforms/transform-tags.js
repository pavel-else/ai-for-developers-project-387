export function transformTags(tags) {
    return tags.map((tag) => {
        const tag32 = tag;
        // Support both native 3.2 fields and x-oai- prefixed extensions for 3.0/3.1
        const summary = tag32.summary ?? tag["x-oai-summary"];
        const kind = tag32.kind ?? tag["x-oai-kind"];
        const parent = tag32.parent ?? tag["x-oai-parent"];
        return {
            name: tag.name,
            description: tag.description,
            externalDocs: tag.externalDocs?.url || tag.externalDocs?.description
                ? {
                    url: tag.externalDocs.url,
                    description: tag.externalDocs.description,
                }
                : undefined,
            parent,
            summary,
            kind,
        };
    });
}
//# sourceMappingURL=transform-tags.js.map